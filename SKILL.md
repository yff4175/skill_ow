---
name: "ow-workshop-syntax"
description: "Overwatch Workshop (.ow) syntax & writing conventions from XHanL/overwatch-workshop. Invoke when writing/debugging workshop rules, generating .ow code, or needing action/value/constant references. 守望先锋工坊代码编写规范，编写调试工坊规则时调用。"
---

# 守望先锋工坊（Overwatch Workshop）代码编写规范

> 本规范提取自 GitHub 仓库 [XHanL/overwatch-workshop](https://github.com/XHanL/overwatch-workshop)（VS Code 扩展 v3.11+）的权威语法定义 `syntaxes/ow.tmLanguage.json`、数据文件 `src/web/model.ts`，并以真实工坊代码 `dm.ow` 为示例参照校正。
> 工坊代码使用**中文关键词**，编写时必须遵循以下结构与约定，否则工坊导入时会解析失败。

## 1. 文件类型与基础

- 扩展名：`.ow`（也支持 `.owl` / `.watch`）
- 编码：UTF-8（含大量中文关键词）
- 缩进：**制表符**，显示大小建议 4
- 注释：行注释 `// ...`；块注释 `/* ... */`
- 字符串：双引号 `"..."`，支持转义 `\n \t \\ \"` 等，支持格式占位符 `{0} {1} {2}`（用于 `自定义字符串`）
- 数字：整数或小数，可带负号，如 `0`、`150`、`-3.14`、`7.500`
- **标点符号（关键）**：代码结构标点必须用**英文半角**——括号 `()`、逗号 `,`、分号 `;`、引号 `"`、运算符 `==`/`!=`/`+`/`-` 等。**禁止**用中文全角 `（）`、`，`、`；`、`“”`，否则导入会解析失败。唯一例外：字符串**内容**（双引号内的显示文本）可用任意中文标点。注意：冒号在变量声明 `0: A` 与结构关键字 `全局:`/`玩家:` 后用英文半角 `:`。

## 2. 顶层结构

一个 `.ow` 文件按以下顺序组织（`设置` 可省略，`变量`/`子程序` 可省略，至少含一个 `规则`）：

```
设置
{
}

变量
{
    全局:
        0: A
        1: MyGlobalVar
    玩家:
        0: A
        1: MyPlayerVar
}

子程序
{
    0: Sub0
    1: MySub
}

规则("规则名称")
{
    事件
    {
        ...
    }
    条件
    {
        ...
    }
    动作
    {
        ...
    }
}
```

## 3. 声明块

### 3.1 变量声明
```
变量
{
    全局:
        0: A
        1: Score
    玩家:
        0: A
        1: Kills
}
```
- 变量以**索引: 名称**形式声明，索引从 `0` 开始；索引可**不连续**（如 `5: ItemName`、`28: ROOM`，真实代码常见跳号）
- 全局变量归属于游戏本身，玩家变量归属于特定玩家
- 名称用字母/下划线开头（`[_a-zA-Z][_a-zA-Z0-9]*`）
- 默认变量名为 `A`（索引 0）

### 3.2 子程序声明
```
子程序
{
    0: Sub0
    1: HandleSpawn
}
```
- 同样以 `索引: 名称` 声明，默认名 `Sub0`；索引可跳号

### 3.3 设置
```
设置
{
}
```
- 通常为空块，用于工坊导入导出的设置映射

## 4. 规则结构

```
规则("规则名称")
{
    事件
    {
        事件类型;
        队伍;
        玩家或英雄;
    }
    条件
    {
        条件表达式;
        条件表达式;
    }
    动作
    {
        动作名(参数1, 参数2);
        动作名(参数);
    }
}
```

**关键约定（违反会导致解析失败）：**
- 规则名用**双引号**包裹：`规则("我的规则")`
- 可加 `禁用` 前缀禁用整条规则（`禁用` 与 `规则` 之间有空格）：`禁用 规则("已禁用") { ... }`
- **花括号 `{` 和 `}` 必须单独成行**，行尾花括号等非标准写法会导致解析不准确
- `事件` 块内每项以**分号 `;`** 结尾
- `条件` 块内每项以**分号 `;`** 结尾（与导出格式一致；语法亦容忍逗号 `,`，但真实工坊导出代码一律用 `;`）
- `动作` 块内每项以**分号 `;`** 结尾
- 三个子块顺序固定：`事件` → `条件` → `动作`（`条件` 可省略）
- `事件`/`条件`/`动作` 三个子块之间可空行，块内可穿插纯字符串"注释行"（如 `"等待时间"`，会被语法忽略）

## 5. 事件（Event）

事件块由「事件类型 + 队伍 + 玩家/英雄」组成（`子程序` 事件类型除外，它接子程序名）。

### 5.1 事件类型
| 事件类型 | 说明 |
|---|---|
| `持续 - 全局` | 全局持续事件，每帧执行 |
| `持续 - 每名玩家` | 每个玩家每帧执行（需接队伍+玩家） |
| `玩家造成伤害` | 玩家造成伤害时 |
| `玩家造成最后一击` | 玩家取得最后一击时 |
| `玩家造成治疗` | 玩家造成治疗时 |
| `玩家造成击退` | 玩家造成击退时 |
| `玩家阵亡` | 玩家死亡时 |
| `玩家参与消灭` | 玩家参与击杀时 |
| `玩家加入比赛` | 玩家加入时 |
| `玩家离开比赛` | 玩家离开时 |
| `玩家受到治疗` | 玩家被治疗时 |
| `玩家受到击退` | 玩家被击退时 |
| `玩家受到伤害` | 玩家受到伤害时 |
| `子程序` | 由 `调用子程序` 触发，后接子程序名而非队伍/玩家 |

### 5.2 队伍
`队伍1`（蓝）/ `队伍2`（红）/ `双方`（两支队伍或非团队模式的所有玩家）

### 5.3 玩家/英雄
- 栏位：`栏位 0` ~ `栏位 11`、`全部`
- 英雄名（直接写中文）：`安娜`、`艾什`、`巴蒂斯特`、`堡垒`、`布丽吉塔`、`末日铁拳`、`D.Va`、`回声`、`源氏`、`破坏球`、`半藏`、`渣客女王`、`狂鼠`、`雾子`、`卢西奥`、`卡西迪`、`美`、`天使`、`莫伊拉`、`奥丽莎`、`法老之鹰`、`死神`、`莱因哈特`、`路霸`、`西格玛`、`索杰恩`、`士兵：76`、`黑影`、`秩序之光`、`托比昂`、`猎空`、`黑百合`、`温斯顿`、`查莉娅`、`禅雅塔`、`拉玛刹`、`生命之梭`、`伊拉锐`、`毛加`、`探奇`、`朱诺`、`骇灾`、`弗蕾娅`、`无漾`、`斩仇`、`金驭`、`安燃`、`埃姆雷`、`瑞稀`、`飞天猫`、`西拉`、`死怨`

### 5.4 规则执行语义（条件与触发次数）

规则的执行次数由「事件类型 + 是否有 `条件` 块」共同决定。**这是工坊最易踩坑的语义之一**：

| 事件类型 | 无 `条件` 块（空条件） | 有 `条件` 块 |
|---|---|---|
| 事件型（`玩家阵亡`/`玩家加入比赛`/`玩家造成伤害` 等） | 事件触发时执行 1 次 | 事件触发且条件满足时执行 1 次 |
| `持续 - 全局` / `持续 - 每名玩家` | 动作序列执行 1 次后停止（**不会自动循环**） | 条件从「假」变为「真」时执行 1 次（**边沿触发**；条件持续为真不会重复执行） |

**核心要点：**
- **空条件（无 `条件` 块）的持续规则只执行 1 次**：动作序列跑完后不会重新触发，因为没有条件边沿变化。
- **有条件的持续规则**：每次条件从「不满足」变为「满足」时，动作序列执行 1 次。要让规则反复执行，需在动作末尾把条件变回「假」，或使用 `循环`/`根据条件循环`/`While` 等动作。

**让持续规则反复执行的两种方式：**

```
// 方式 1：条件翻转实现边沿触发（推荐，可控）
规则("反复执行 - 条件翻转")
{
    事件
    {
        持续 - 全局;
    }
    条件
    {
        全局.Run == 真;
    }
    动作
    {
        // ... 你的逻辑 ...
        全局.Run = 假;       // 本轮结束，条件变假
        等待(1, 无视条件);
        全局.Run = 真;       // 条件再次变真 → 触发下一轮
    }
}

// 方式 2：用 循环 动作（空条件时也能反复执行）
规则("反复执行 - 循环动作")
{
    事件
    {
        持续 - 全局;
    }
    动作
    {
        While(真);
            // ... 你的逻辑 ...
            等待(1, 无视条件);
        End;
    }
}
```

> ⚠️ 注意区分：`循环;`（无条件重新执行本规则动作序列）与 `根据条件循环(条件);`（条件为真时重新执行）与 `While(条件); ... End;`（条件为真时循环块内动作）。

## 6. 动作（Action）

动作出现在 `动作` 块内，格式为 `动作名(参数1, 参数2, ...);`。控制流关键字 `If`/`Else If`/`Else`/`End`/`While`/`For ...` 为英文。下面按类别列出（**完整列表见 `references/ow.tmLanguage.json` 的 `actionNames`**）：

### 6.1 控制流
`If`、`Else If`、`Else`、`End`、`While`、`For 玩家变量`、`For 全局变量`、`根据条件中止`、`根据条件循环`、`根据条件跳过`、`如条件为"真"则中止`、`如条件为"真"则循环`、`如条件为"假"则中止`、`如条件为"假"则循环`、`等待`、`等待直到`、`循环`、`跳过`、`中止`、`中断`、`调用子程序`、`返回大厅`、`开始规则`、`重新开始规则`

**控制流语法要点（来自真实代码）：**

```
If(条件);
    动作;
Else If(条件);
    动作;
Else;
    动作;
End;
```
- `If`/`Else If` 后跟 `(条件)`，`Else` 与 `End` 不接参数，均以 `;` 结尾
- `While(条件); ... End;` 同理

```
For 全局变量(变量名, 起始, 结束, 步长);
    动作;     // 循环体内用 单次赋值(全局.变量名) 取当前值，避免每帧重算
End;
```

> ⚠️ **`For 玩家变量` 与 `For 全局变量` 签名不同（参数数量不同，易踩坑）：**
>
> | 控制流 | 签名 | 参数数量 |
> |---|---|---|
> | `For 全局变量` | `For 全局变量(变量名, 起始, 结束, 步长); ... End;` | 4 个 |
> | `For 玩家变量` | `For 玩家变量(玩家, 变量名, 起始, 结束, 步长); ... End;` | **5 个**（比全局多一个"玩家"参数） |
>
> **原因**：玩家变量归属于特定玩家，循环时必须指明"是哪个玩家的变量"；全局变量只有一个命名空间，无需指定玩家。
>
> - ✅ 正确：`For 玩家变量(事件玩家, I, 0, 数量(事件玩家.NearbyPlayers), 1);`
> - ❌ 错误：`For 玩家变量(I, 0, 数量(事件玩家.NearbyPlayers), 1);`（漏掉第一个"玩家"参数，会解析失败）

**`等待` 与 `等待直到`（高频使用，参数必须正确）：**
```
等待(秒数, 等待条件);
    // 等待条件 ∈ { 无视条件, 当为"假"时中止, 当为"真"时中止, 当为"真"时重新开始 }
    // "当为'假'时中止" 表示每帧重判条件，一旦为假则中止本规则
等待(0.250, 当为"假"时中止);
等待(0, 无视条件);

等待直到(条件, 超时秒数);
    // 每帧检查 条件，为真则继续；超过 超时秒数 也继续
等待直到(已重生(事件玩家), 5399);
等待直到(全局.game_0[0] <= 0, 全局.game_0[0]);
```

**禁用单条动作：** 在动作名前加 `禁用 ` 前缀（与禁用规则同理），该动作保留但不执行：
```
禁用 关闭游戏预设通告模式;
禁用 设置移动速度(事件玩家, 150);
```

### 6.2 变量操作
`设置全局变量`、`设置玩家变量`、`修改全局变量`、`修改玩家变量`、`在索引处设置全局变量`、`在索引处设置玩家变量`、`在索引处修改全局变量`、`在索引处修改玩家变量`、`追踪全局变量频率`、`追踪玩家变量频率`、`持续追踪全局变量`、`持续追踪玩家变量`、`停止追踪全局变量`、`停止追踪玩家变量`

> 变量赋值/修改语法（**来自真实代码，必须用点号/索引语法**）：
> - 设置全局变量：`全局.变量名 = 值;`（如 `全局.Score = 100;`）
> - 设置玩家变量：`事件玩家.变量名 = 值;`
> - 设置数组索引：`全局.变量名[索引] = 值;`、`事件玩家.变量名[索引] = 值;`
> - 修改全局变量：`全局.变量名 操作 值;`（操作符 `+=`、`-=`、`*=`、`/=` 等）
> - 修改玩家变量：`事件玩家.变量名 操作 值;` 或 `修改玩家变量(玩家, 变量名, 操作, 值);`
> - 修改数组索引：`全局.变量名[索引] 操作 值;`（如 `事件玩家.Special_state[5] = 真;`）
> - 操作选项（中文）：`加`/`减`/`乘`/`除`/`乘方`/`取模`/`添加至数组`/`从数组中移除`/`根据值从数组中移除`/`根据索引从数组中移除`
>
> 示例：
> ```
> 全局.Time -= 1;
> 事件玩家.Special_state = 数组(0, 0, 0, 0, 0, 真, 真, 真, 7.500, 假, 0, 0, 0);
> 修改全局变量(RecordPos_1, 根据索引从数组中移除, 0);
> 修改玩家变量(事件玩家, BUFF, 添加至数组, 数组(数组(事件玩家.BUFFINDEX, 4, 1, 50, 全局.game_0[0])));
> ```

### 6.3 玩家属性与状态
`设置移动速度`、`设置最大生命值`、`设置玩家生命值`、`设置最大弹药`、`设置弹药`、`设置跳跃垂直速度`、`设置引力`、`设置弹道引力`、`设置弹道速度`、`设置慢动作`、`设置朝向`、`设置不可见`、`设置瞄准速度`、`设置状态`、`清除状态`、`设置武器`、`设置终极技能充能`、`设置技能资源`、`设置技能冷却`、`设置技能充能`、`设置造成伤害`、`设置造成治疗`、`设置造成的击退`、`设置受到伤害`、`设置受到治疗`、`设置受到的击退`、`设置玩家分数`、`设置队伍分数`、`设置比赛时间`、`设置玩家可选的英雄`、`设置目标点描述`、`设置启用装填`/`设置启用跳跃`/`设置启用近战攻击`/`设置启用技能 1`/`设置启用技能 2`/`设置启用终极技能`/`设置启用蹲下`、`设置主要攻击模式启用`、`设置辅助攻击模式启用`、`设置最大复生时间`、`设置地形消灭者玩家`

常用签名：
- `设置状态(目标, 来源, 状态常量, 持续时间);` → `设置状态(事件玩家, 事件玩家, 相移, 全局.game_0[0]);`
- `设置移动速度(玩家, 百分比);` → `设置移动速度(事件玩家, 150);`
- `设置玩家可选的英雄(玩家, 英雄数组);` → `设置玩家可选的英雄(事件玩家, 数组(英雄(查莉娅), 英雄(回声), 英雄(士兵：76)));`
- `设置比赛时间(秒数);` → `设置比赛时间(9999);`

### 6.4 玩家动作
`传送`、`复生`、`复活`、`击杀`、`施加推力`、`调整玩家队伍`、`绑定玩家`、`解除绑定`、`按下按键`、`开始按下按钮`/`停止按下按钮`、`开始朝向`/`停止朝向`、`开始加速`/`停止加速`、`开始镜头`/`停止镜头`、`开始调整玩家大小`/`停止调整玩家大小`、`开始调整障碍大小`/`停止调整障碍大小`、`开始强制设置玩家位置`/`停止强制设置玩家位置`、`开始强制设置玩家轮廓`/`停止强制设置玩家轮廓`、`开始强制重生室`/`停止强制重生室`、`开始强制玩家选择英雄`/`停止强制玩家选择英雄`、`开始为机器人强制设置名称`/`停止为机器人强制设置名称`、`生成机器人`、`移除机器人`、`移除所有机器人`、`移除玩家`、`预加载英雄`、`重置玩家英雄可选状态`、`取消主要动作`、`开启与玩家的移动碰撞`/`取消与玩家的移动碰撞`、`开启与环境的移动碰撞`/`取消与环境的移动碰撞`、`前往集结英雄`、`交流`、`继续`

常用签名：
- `传送(玩家, 位置);` 位置可为矢量或矢量数组（随机取一点）：
  `传送(事件玩家, 矢量(-25.300, 6.500, -88.900));` / `传送(事件玩家, 数组随机取值(全局.ROOM));`
- `开始镜头(玩家, 眼睛位置, 朝向位置, 视野);` → `开始镜头(事件玩家, 眼睛位置(事件玩家) + ..., 事件玩家 + 面朝方向(事件玩家) * 1000, 100);`
- `停止镜头(玩家);`
- `调用子程序(子程序名);`
- `开始强制玩家选择英雄(玩家, 英雄);` → `开始强制玩家选择英雄(事件玩家, 事件玩家.CurrentHero);`（第 2 参数指定强制切换到的英雄；省略则不生效）
- `停止强制玩家选择英雄(玩家);`
- `施加推力(玩家, 方向, 速度, 相对, 相反运动);`（**5 参数**）→ `施加推力(事件玩家, 面朝方向(事件玩家), 15, 至地图, 取消相反运动);`
  - 方向：**矢量**，推力方向矢量（`施加推力` 内部会归一化方向，所以幅值不重要，方向才是关键）
  - 速度：**数字**，推力大小（如 `15`、`全局.PushForce`、`50`）
  - 相对：`至地图`（世界坐标系）/ `至玩家`（相对玩家朝向）/ `至摄像机`（相对摄像机）（见 §8）
  - 相反运动：`合并相反运动` / `取消相反运动` / `取消相反运动XYZ`（见 §8）
  - 向外爆炸推力示例（从落点指向玩家方向 + 向上分量）：
    `施加推力(全局.TargetPlayer, 方向(全局.LandPos, 全局.TargetPlayer) + 矢量(0, 5, 0), 全局.PushForce, 至地图, 取消相反运动);`
    - `方向(落点, 玩家)` 返回从落点指向玩家的带长度矢量（等于 `玩家 - 落点`）
    - `施加推力` 内部会归一化方向，所以不需要手动 `归一化`
    - `+ 矢量(0, 5, 0)` 叠加向上分量让推力偏上
  - ⚠️ 玩家对象**可以直接参与矢量运算**：`玩家 - 位置矢量`、`玩家 + 矢量(0, 5, 0)` 等写法合法，工坊会自动取玩家位置参与运算。**不要用 `位置(玩家)` 转换**（`位置()` 不是此处的正确写法，会出错）。需要头部位置时用 `眼睛位置(玩家)`。

### 6.5 文本
`创建HUD文本`、`创建地图文本`、`创建进度条HUD文本`、`创建进度条地图文本`、`大字体信息`、`小字体信息`、`消除HUD文本`、`消除地图文本`、`消除进度条HUD文本`、`消除进度条地图文本`、`消除所有HUD文本`、`消除所有地图文本`、`消除所有进度条HUD文本`、`消除所有进度条地图文本`

常用签名（**参数顺序来自真实代码**）：
```
创建HUD文本(可见目标, 顶部标题, 左侧文本, 右侧文本, 位置, 排序ID, 颜色1, 颜色2, 颜色3, 可见性, 可见度);
    // 位置 ∈ {左边, 顶部, 右边, 底部}
创建HUD文本(所有玩家(队伍1), 自定义字符串(""), 自定义字符串("胜利条件..."), 自定义字符串("..."),
    左边, 12, 颜色(白色), 颜色(白色), 颜色(白色), 可见和字符串, 默认可见度);

// ⚠️ 文本显示位置由参数槽位决定，不是"位置"参数：
//   - 第2参数（顶部标题）→ 显示在屏幕最上方（标题区）
//   - 第3参数（左侧文本）→ 显示在屏幕左侧（正文区）
//   - 第4参数（右侧文本）→ 显示在屏幕右侧
// 想让整段文本显示在顶部，把内容放第2参数，第3、4参数留空：
创建HUD文本(事件玩家, 事件玩家.InvText, 空, 空, 左边, 5, 颜色(白色), ...);

创建地图文本(可见目标, 文本, 位置, 大小, 截取, 可见性, 颜色, 可见度);
    // 位置常用 逐帧更新(眼睛位置(玩家) + ...) 动态更新
创建地图文本(本地玩家, 自定义字符串("{0}", 全局.tip), 逐帧更新(眼睛位置(本地玩家) + 100 * 面朝方向(本地玩家)),
    1.500, 不要截取, 可见，位置，字符串和颜色, 颜色(黄色), 默认可见度);

创建进度条地图文本(可见目标, 进度值, 文本, 位置, 半径, 截取, 颜色1, 颜色2, 可见性, 可见度);

大字体信息(可见目标, 值或文本);     // 大字号居中提示
小字体信息(可见目标, 值或文本);     // 小字号提示，常用 2 参数形式
    小字体信息(本地玩家, 全局.game_0[0]);
    小字体信息(事件玩家, 自定义字符串("击杀: {0}", 事件玩家.Kills));
消除HUD文本(文本ID);               // 文本ID 来自 上一个文本ID
```

### 6.6 效果与弹道
`创建效果`、`创建图标`、`创建光束效果`、`创建弹道`、`创建追踪弹道`、`创建弹道效果`、`播放效果`、`消除效果`、`消除图标`、`消除所有效果`、`消除所有图标`

```
创建效果(可见目标, 形状, 颜色, 位置, 半径, 可见性);
创建效果(本地玩家, 环, 颜色(绿色), 首个(全局.Portal[3]), 3, 可见，位置和半径);

播放效果(可见目标, 效果类型, 颜色, 位置, 半径);
播放效果(所有玩家(所有队伍), 有害爆炸, 颜色(橙色), 全局.LandPos, 全局.PushRadius);
```

- **`创建效果` vs `播放效果`**：
  - `创建效果`：创建持久存在的静态效果（球、环、光柱等），需用 `消除效果` 清理，否则会残留。适合持续显示的区域标记、球体跟随等
  - `播放效果`：一次性瞬时特效（爆炸、冲击），自动消失无需清理。适合爆炸、命中、临时冲击视觉
- **效果类型常量**（`播放效果` 第 2 参数）：`有益爆炸`、`有害爆炸`、`有益光环`、`有害光环`、`有益光束`、`有害光束`、`有益选择效果`、`有害选择效果`、`D.Va自毁爆炸效果`、`温斯顿原始暴怒效果`、`托比昂热力过载效果` 等
- **形状常量**（`创建效果` 第 2 参数）：`球`、`环`、`细环`、`宽环`、`光柱`、`光晕`、`星形`、`螺旋`、`箭头`、`圆圈`、`骷髅`、`感叹号`、`问号`、`对号`、`加号`、`火花`、`火焰`、`云` 等

### 6.7 治疗/伤害/助攻
`治疗`、`伤害`、`开始治疗调整`/`停止治疗调整`/`停止所有治疗调整`、`开始伤害调整`/`停止伤害调整`/`停止所有伤害调整`、`开始持续治疗`/`停止持续治疗`/`停止所有持续治疗`、`开始持续伤害`/`停止持续伤害`/`停止所有持续伤害`、`开始助攻`/`停止助攻`/`停止所有助攻`、`为玩家添加生命池`、`移除玩家的生命池`、`移除玩家的所有生命值`、`开始修改英雄语音`/`停止修改英雄语音`

### 6.8 阈值
`开始转换阈值`/`停止转换阈值`、`开始限制阈值`/`停止限制阈值`、`开始定向阈值`/`停止定向阈值`、`添加至现有阈值`、`替换现有阈值`

### 6.9 比赛/游戏/UI
`开始游戏模式`、`重新开始比赛`、`宣告玩家胜利`、`宣告队伍胜利`、`宣告回合胜利`、`宣布比赛为平局`、`宣布回合为平局`、`比赛时间暂停`、`比赛时间继续`、`修改玩家分数`、`修改队伍分数`、`启用语音聊天`/`禁用语音聊天`、`启用文字聊天`/`禁用文字聊天`、`启用死亡回放时目标的HUD`/`禁用死亡回放时目标的HUD`、`启用查看器录制`/`禁用查看器录制`、`对所有玩家启用死亡回放`/`对所有玩家禁用死亡回放`、`开启游戏预设音乐模式`/`关闭游戏预设音乐模式`、`开启游戏预设完成条件`/`关闭游戏预设完成条件`、`开启游戏预设通告模式`/`关闭游戏预设通告模式`、`开启游戏预设计分模式`/`关闭游戏预设计分模式`、`开启游戏预设复生模式`/`关闭游戏预设复生模式`、`显示游戏模式HUD`/`隐藏游戏模式HUD`、`显示游戏模式地图UI`/`隐藏游戏模式地图UI`、`显示英雄HUD`/`隐藏英雄HUD`、`显示姓名板`/`隐藏姓名板`、`显示信息`/`隐藏信息`、`显示消灭提示`/`隐藏消灭提示`、`显示计分板`/`隐藏计分板`、`记入查看器`、`可用按钮`、`禁用按钮`

常用签名：
- `关闭游戏预设复生模式(玩家);`（带参数）vs `关闭游戏预设完成条件;`（不带参数）
- `隐藏姓名板(可见目标, 被隐藏目标);`（2 参数）
- `禁用按钮(玩家, 按钮);` → `禁用按钮(事件玩家, 按钮(辅助攻击模式));`

## 7. 值与条件（Value / Condition）

值函数可出现在 `条件` 块和动作参数中，格式为 `函数名(参数1, 参数2)`。条件块内多个条件用 `;` 分隔。**完整列表见 `references/ow.tmLanguage.json` 的 `conditionNames`**。常用分类：

> ⚠️ **颜色与按钮必须用函数包裹**：颜色常量（`白色`、`红色`…）须写作 `颜色(白色)`；按钮常量（`技能1`、`蹲下`…）须写作 `按钮(技能1)`、`按钮(蹲下)`。直接写 `白色` 或 `技能1` 在动作参数中不会被识别为对应类型。

### 7.1 逻辑、比较与运算符
- 比较：`比较(值1, 运算符, 值2)` 或直接中缀 `值1 == 值2`、`值1 != 值2`、`<=`/`>=`/`<`/`>`
- 逻辑中缀：`&&`（与）、`||`（或）、`!`（非）→ `在视线内(...) && 相距距离(...) <= 10`、`!事件玩家.Special_state[0]`
- 三元：`条件 ? 真值 : 假值`，可嵌套→ `所在队伍(本地玩家) == 队伍1 ? (...) : (...)`
- `If-Then-Else(If, Then, Else)`：三元的函数形式，`If` 为真返回 `Then`，否则返回 `Else` → `If-Then-Else(条件, 真值, 假值)`
- `对任意为"真"(数组, 假)`、`对全部为"真"(数组, 假)`、`真`、`假`
- ⚠️ **取负值必须用 `-1 * 值`，不可直接写 `-值`**：工坊语法解析器对前缀负号 `-值` 的支持不稳定，导入后可能被解析为减法而非负号。规范写法：`-1 * 全局.Gravity * 0.016`（不要写 `-全局.Gravity * 0.016`）；字面量负数（如 `-3.500`、`-25.300`）可正常使用，不受此限。
  - ❌ 错误：`矢量(0, 0, -全局.Gravity * 0.016)`
  - ✅ 正确：`矢量(0, 0, -1 * 全局.Gravity * 0.016)`

### 7.2 数学
`绝对值`、`平方根`、`取整`、`较大`、`较小`、`余数`、`角度的正弦值`/`余弦值`/`正切值`、`弧度的正弦值`/`余弦值`/`正切值`、`以角度为单位的反正弦值`/`反余弦值`/`反正切值`、`以弧度为单位的反正弦值`/`反余弦值`/`反正切值`、`标量积`、`矢量积`、`矢量间夹角`、`归一化`、`幅值`、`相距距离`、`角度差`

常用签名：
- `取整(值, 取整模式);` 取整模式 ∈ {`上`, `下`, `至最近`} → `取整(总计消耗时间 - 单次赋值(总计消耗时间), 上)`
- `较大(值1, 值2);` / `较小(值1, 值2);` → `较大(-3.500, 相距距离(...))`
- `余数(值1, 值2);` —— 取模运算（等价于 `值1 % 值2`），左边除以右边的余数；右边为 0 时结果为 0
- `相距距离(位置1, 位置2);`、`绝对值(值);`、`平方根(值);`
- `归一化(矢量);` → `归一化(全局.TargetPlayer - 全局.LandPos)` 返回单位方向矢量（保留原方向，幅值=1）
- `方向(起点, 终点);` → `方向(全局.LandPos, 全局.TargetPlayer)` 返回**带长度的矢量**（等于 `终点 - 起点`，从起点指向终点的矢量，含距离长度）
  - ⚠️ `方向(起点, 终点)` = `终点 - 起点`，返回的是带长度的矢量，不是单位矢量，也不是角度
  - 与 `归一化(A - B)` 的区别：`方向(A, B)` 保留长度（= 距离），`归一化(A - B)` 长度=1
  - 用于推力方向（`施加推力` 内部会归一化方向）：`施加推力(玩家, 方向(落点, 玩家) + 矢量(0, 5, 0), 力, 至地图, 取消相反运动)`

### 7.3 矢量与方向
`矢量(x, y, z)`、`向量(开始位置, 结束位置)`、`X方向分量`/`Y方向分量`/`Z方向分量`、`本地矢量`、`地图矢量`、`方向`、`水平朝向角度`、`垂直朝向角度`、`水平方向夹角`、`垂直方向夹角`、`与此角度的相对方向`、`与此方向的水平角度`/`垂直角度`、`面朝方向`、`眼睛位置`、`最近的可行走位置`、`逐帧更新`

常用签名：
- `矢量(x, y, z);` → `矢量(-25.300, 6.500, -88.900)`
- `向量(开始位置, 结束位置);` —— 从一个位置到另一个位置的位移矢量（等于 `结束位置 - 开始位置`），与 `方向(起点, 终点)` 等价
- `地图矢量(方向, 玩家, 旋转模式);` **3 参数必填**
  → 方向 ∈ {`左`, `右`, `前`, `后`, `上`, `下`}
  → 第 3 参数是**常量**，只有两个取值：`旋转`（仅旋转方向，不改变坐标系）/ `旋转并转换`（旋转并转换到世界坐标系）
  → `地图矢量(前, 事件玩家, 旋转)` —— **玩家朝向的前方向（世界坐标系，OW 中 前 = Z 轴）**，常用于发射方向
  → ⚠️ 第 3 参数**不可省略**，也不是矢量值（不要写 `矢量(0, 0, 0)`）
- `本地矢量(方向, 玩家, 旋转模式);` 同理（基于玩家本地坐标），3 参数必填
- `逐帧更新(值);` —— 包裹位置等参数，使其每帧重新求值（用于动态 `创建地图文本` 位置等）
  → `逐帧更新(眼睛位置(本地玩家) + 100 * 面朝方向(本地玩家))`

> ⚠️ **守望先锋坐标系（关键！）**：OW 使用 **Y 轴向上**的左手坐标系：
> - **X 轴**：左右（侧向）
> - **Y 轴**：上下（**向上为 +Y**，向下为 -Y）
> - **Z 轴**：前后（**向前为 +Z**，向后为 -Z）
>
> 因此：
> - **向上/向下分量**：`矢量(0, 值, 0)`（Y 分量，不是 Z！）
> - **向前/向后分量**：`矢量(0, 0, 值)`（Z 分量）
> - **重力**作用于 Y 轴：`矢量(0, -1 * Gravity * dt, 0)`（负 Y 向下）
> - **地面检测**用 `Y方向分量(位置)` 比较，不是 Z
> - `地图矢量(前, 玩家)` 返回的矢量主要 Z 分量非 0；`地图矢量(上, 玩家)` 返回的矢量 Y 分量为正
>
> ❌ 常见错误：把"向上"写成 `矢量(0, 0, 1)`（这是向前，不是向上）
> ✅ 正确：向上用 `矢量(0, 1, 0)`，向前用 `矢量(0, 0, 1)`

> ⚠️ **`面朝方向(玩家)` vs `地图矢量(前, 玩家, 旋转)` 的区别**（决定抛物线是否受俯仰控制）：
> - `面朝方向(玩家)`：返回玩家**视线方向（含垂直俯仰分量）**。乘速度作发射方向时：
>   - 玩家抬头 → 球向斜上方飞 → 落点远
>   - 玩家平视 → 标准抛物线
>   - 玩家低头 → 球向斜下方飞 → 落点近
>   - **适合需要视角控制落点距离**的发射物（榴弹、抛物线球、玩家自由瞄准的发射）
> - `地图矢量(前, 玩家, 旋转)`：返回**纯水平**的玩家前方向（无 Y 分量），无论玩家抬头低头，水平分量稳定。
>   - **适合固定水平方向**的发射（直线子弹、平射弹道、不希望俯仰影响的弹幕）
> - 关键区分：**抛物线发射用 `面朝方向`**（视角控制落点）；**平射用 `地图矢量(前, ..., 旋转)`**（固定水平）

### 7.4 玩家状态
`正在移动`、`正在跳跃`、`正在装填`、`正在站立`、`正在蹲下`、`正在空中`、`正在近战攻击`、`正在使用主要武器`/`辅助武器`/`终极技能`/`技能 1`/`技能 2`、`正在使用英雄`、`正在使用表情交流`/`喷漆交流`/`语音交流`、`正在与人交流`、`正在交流(玩家, 收到)`、`正在人格复制`、`正在防守`、`正在集结英雄`、`正在等待玩家`、`正在设置`、`在墙上`、`在地面上`、`在目标点上`、`在重生室中`、`在视线内`、`在视野内`、`存活`、`死亡`、`已重生`、`按钮被按下`、`具有状态`、`是否是机器人`

常用签名：
- `按钮被按下(玩家, 按钮);` → `按钮被按下(事件玩家, 按钮(互动)) == 真`
- `在视线内(观察者, 目标, 屏障阻挡视线);` 屏障阻挡视线 ∈ {`屏障不会阻挡视线`, `所有屏障阻挡视线`, `敌方屏障阻挡视线`, `表面及全部屏障`, `表面及敌方屏障`}
- `在视野内(玩家, 目标);`、`具有状态(玩家, 状态);`、`是否是机器人(玩家);`、`存活(玩家);`、`死亡(玩家);`、`已重生(玩家);`

### 7.5 玩家属性
`生命值`、`最大生命值`、`最大弹药`、`速度`、`速率`、`水平速度`、`垂直速度`、`指定方向速度`、`面朝方向`、`眼睛位置`、`所选位置`、`高度`、`所在队伍`、`所用英雄`、`正在使用英雄`、`正在复制的英雄`、`终极技能充能百分比`、`技能充能`、`技能冷却时间`、`技能资源`、`弹药`、`阈值`、`助攻数量`、`消灭数`、`死亡数`、`最后一击数`、`分数`、`栏位`、`类型的最大生命值`/`类型的生命值`、`标准化生命值`、`玩家数据`、`玩家英雄数据`、`伤害调整数量`、`治疗调整数量`、`持续伤害数量`、`持续治疗数量`

常用签名（多数接玩家参数）：`生命值(玩家)`、`所在队伍(玩家)`、`所用英雄(玩家)`、`所选位置(玩家)`、`速度(玩家)`、`终极技能充能百分比(玩家)`
- ⚠️ 获取玩家位置用 `所选位置(玩家)`（返回矢量），**不是** `位置(玩家)`；获取移动速度用 `速度(玩家)` 或 `速率(玩家)`，**不是** `移动速度(玩家)`（`移动速度` 是动作 `设置移动速度` 的名字，不是值函数）
- `技能充能(玩家, 按钮)` / `技能冷却时间(玩家, 按钮)` / `技能资源(玩家, 按钮)`：均需指定按钮（如 `按钮(技能1)`），返回充能次数 / 冷却秒数 / 资源百分比
- `高度(玩家)`：玩家当前距表面高度（米），在表面上为 0
- `玩家数据(玩家, 数据)`、`玩家英雄数据(玩家, 英雄, 数据)`：读取玩家/英雄数据面板数据
- `伤害调整数量`、`治疗调整数量`、`持续伤害数量`、`持续治疗数量`：无参，返回对应动作启动的副本当前数量

### 7.6 玩家集合
`所有玩家(队伍)`、`所有存活玩家`、`所有死亡玩家`、`所有重装英雄`/`所有支援英雄`/`所有输出英雄`、`所有目标点内玩家`/`所有目标点外玩家`、`玩家数量`、`存活玩家数量`、`死亡玩家数量`、`栏位数量`、`此栏位的玩家`、`距离最近的玩家`、`距离最远的玩家`、`距离准星最近的玩家`、`视角中的玩家`、`范围内玩家`、`携带旗帜的玩家`、`选择英雄的玩家`、`本地玩家`、`事件玩家`、`主机玩家`、`攻击方`、`被攻击方`、`受治疗者`、`治疗者`、`事件伤害`、`事件治疗`、`事件技能`、`事件方向`、`事件暴击`、`事件为急救包`、`事件为环境事件`

常用签名：
- `所有玩家(队伍);` 队伍 ∈ {`队伍1`, `队伍2`, `双方`, `所有队伍`} → `所有玩家(所有队伍)`
- `玩家数量(队伍);` → `玩家数量(队伍1)`
- `范围内玩家(中心, 半径, 队伍, 表面或视线检查);` → `范围内玩家(事件玩家, 12, 所有队伍, 关闭)`（第 3 参数是**队伍**常量，不是玩家数组；第 4 参数是视线检查常量，不是布尔 `假`/`真`）
  - 视线检查常量 ∈ {`关闭`, `表面`, `屏障不会阻挡视线`, `所有屏障阻挡视线`, `敌方屏障阻挡视线`, `表面及全部屏障`, `表面及敌方屏障`}（见 §8）
  - ❌ 错误：`范围内玩家(..., 所有玩家(所有队伍), 假)` —— 第 3 参数用了玩家数组，第 4 参数用了布尔
  - ✅ 正确：`范围内玩家(..., 所有队伍, 关闭)`
- `此栏位的玩家(栏位号, 队伍);` → `此栏位的玩家(单次赋值(全局.I5), 队伍1)`
- `距离最近的玩家(玩家, 队伍);`、`选择英雄的玩家(英雄, 队伍);`
- `本地玩家`、`事件玩家`、`攻击方`、`被攻击方`、`受治疗者`、`治疗者` 无参

### 7.7 英雄
`英雄(英雄名)`、`英雄数量(英雄, 队伍)`、`英雄图标字符串(英雄)`、`技能图标字符串(英雄, 按钮)`、`全部英雄`、`可用英雄`

- `英雄(查莉娅);`、`英雄(D.Va);`
- `英雄图标字符串(英雄(源氏));`
- `技能图标字符串(英雄(源氏), 按钮(技能1));`

### 7.8 队伍与游戏
`队伍`、`所有队伍`、`对方队伍`、`团队得分`、`占领要点模式正在得分的队伍`、`占领要点模式占领点解锁`、`占领要点模式得分百分比`、`作为进攻队伍`、`游戏正在进行中`、`游戏模式(模式)`、`当前游戏模式`、`当前地图`、`地图`、`比赛时间`、`比赛结束`、`比赛回合`、`处于回合之间`、`处于非初始状态`、`总计消耗时间`、`目标点占领百分比`、`目标点上玩家数量`、`目标是否完成`、`运载目标位置`、`运载目标进度百分比`、`在夺旗模式中开始绝杀局`、`旗帜位置`、`旗帜是否在基地中`、`是否有人携带旗帜`、`重生点(队伍)`、`服务器负载`/`服务器负载平均值`/`服务器负载峰值`、`地图工坊设置组合`/`整数`/`英雄`/`实数`/`开关`

**地图工坊设置（在规则初始化时读取，用于"地图工坊设置"面板）：**
```
全局.Workshop[0] = 地图工坊设置整数(标题, 描述, 默认值, 最小值, 最大值, ?);
    地图工坊设置整数(自定义字符串("地图资源"), 自定义字符串("房间资源上限"), 5, 1, 6, 0);
全局.Workshop[5] = 地图工坊设置开关(标题, 描述, 默认布尔, ?);
    地图工坊设置开关(自定义字符串("制图模式"), 自定义字符串("发送需要治疗"), 假, 0);
// 同理：地图工坊设置实数(...)、地图工坊设置英雄(...)、地图工坊设置组合(...)
```

### 7.9 数组
`数组(...)`、`空数组`、`添加至数组`、`从数组中移除`、`根据值从数组中移除`、`根据索引从数组中移除`、`映射的数组`、`已过滤的数组`、`已排序的数组`、`数组包含`、`数组中的值`、`数组值的索引`、`数组随机取值`、`数组分割`、`首个`、`最后`、`数量`、`当前数组元素`、`当前数组索引`、`单次赋值`、`对象索引`

- 数组索引访问：`数组[索引]`，支持嵌套 `全局.JJHJ[72][0]`、`事件玩家.Special_state[5]`
- `已过滤的数组(数组, 条件);` 条件中用 `当前数组元素` 引用元素：
  `已过滤的数组(全局.Portal[3], 在视线内(本地玩家, 当前数组元素, 屏障不会阻挡视线) && 相距距离(当前数组元素, 本地玩家) <= 10)`
- `已排序的数组(数组, 排序键);` → `已排序的数组(已过滤的数组(...), 相距距离(本地玩家, 当前数组元素))`
- `首个(数组);` / `最后(数组);` / `数量(数组);` / `数组包含(数组, 值);`
- `数组随机取值(数组);` / `从数组中移除(数组, 值);`
- `单次赋值(值);` —— 在一次规则执行内缓存某值（避免每帧重算），常用于 `For` 循环计数器：`单次赋值(全局.I5)`

### 7.10 字符串
`自定义字符串("{0} {1} {2}", 值1, 值2, 值3)`、`字符串("", ...)`、`自定义颜色(颜色)`、`字符串长度`、`字符串替换`、`字符串分割`、`字符串包含`、`字符串字符索引`、`字符串中字符`、`截取字符串`、`输入绑定字符串`、`文本数量`、`图标字符串`、`头像火力全开`、`英雄图标字符串(英雄)`、`技能图标字符串(英雄, 按钮)`

- `自定义字符串("击杀: {0}", 事件玩家.Kills);` —— `{0}`/`{1}`/`{2}` 对应后续参数；不用到的占位用 `空` 填充
- `输入绑定字符串(按钮);` → `输入绑定字符串(按钮(蹲下))` 返回按键的绑定文本

> ⚠️ **`自定义字符串` 最多只支持 3 个占位符 `{0}`/`{1}`/`{2}`**，且值参数不足 3 个时**必须用 `空` 补齐**。超过 3 个占位符时第 4 个及之后的 `{3}`/`{4}` 不生效。
> - ❌ 错误（4个占位符，`{3}` 不生效）：`自定义字符串("[{0}]打开 [{1}]下 [{2}]确认 [{3}]返回", 按键0, 按键1, 按键2, 按键3)`
> - ✅ 正确（拆成两个子字符串，用外层 `自定义字符串("{0}{1}", ...)` 嵌套拼接）：
>   `自定义字符串("{0}{1}", 自定义字符串("[{0}]打开 [{1}]下", 按键0, 按键1, 空), 自定义字符串(" [{0}]确认 [{1}]返回", 按键2, 按键3, 空), 空)`

> ⚠️ **字符串拼接不能用 `+` 运算符，必须用嵌套 `自定义字符串("{0}{1}", 串A, 串B)`**。工坊的字符串类型不支持 `+` 拼接，`+` 只能用于数字/矢量加法。
> - ❌ 错误：`自定义字符串("A") + 自定义字符串("B")`
> - ✅ 正确：`自定义字符串("{0}{1}", 自定义字符串("A"), 自定义字符串("B"), 空)`

> ⚠️ **字符串作为「值/参数」时必须用 `自定义字符串("...")` 包裹，不可直接写字面量 `"..."`**。工坊解析器会把裸字符串字面量当作空位，报「此处应有表达式」。这与 `颜色(白色)`/`按钮(技能1)` 必须用函数包裹是同类规则（见 §9 第 9 条）。
> - ❌ 错误（字符串作数组元素/参数）：`数组("武器", 子数组, 真)`、`小字体信息(事件玩家, "提示")`
> - ✅ 正确：`数组(自定义字符串("武器"), 子数组, 真)`、`小字体信息(事件玩家, 自定义字符串("提示"))`
> - **区分：以下场景仍用裸字符串 `"..."`，不要包裹**：
>   - 规则名：`规则("我的规则")`
>   - 子程序事件名：`子程序; RefreshInv;`（这里是标识符，不是字符串）
>   - 动作块内注释行：`"这是注释"`（独立成行的字符串字面量，被语法忽略）
>   - `自定义字符串` 自身的第一个参数（即字符串内容本身）：`自定义字符串("文本")`

### 7.11 随机与实体
`随机整数`、`随机实数`、`随机数组`、`最后创建的实体`、`最后创建的生命池`、`实体存在`、`实体数量`、`射线命中位置`/`射线命中玩家`/`射线命中法线`、`上一个助攻ID`、`上一个治疗调整ID`、`上一个文本ID`、`上一个伤害调整ID`、`上一个持续治疗效果ID`、`上一个持续伤害效果ID`

> ⚠️ 文本/效果 ID 类值函数均以 `上一个` 开头：`上一个文本ID`（不是 `文本ID`）、`上一个治疗调整ID`、`上一个伤害调整ID`、`上一个持续治疗效果ID`、`上一个持续伤害效果ID`、`上一个助攻ID`。

常用签名：
- `射线命中位置(起点, 终点, 包含玩家数组, 排除玩家数组, 视线检查);`
  - 包含玩家数组：`所有玩家(所有队伍)` = 检测所有玩家；`空` = 不检测玩家（只检测地形）
  - 排除玩家数组：`事件玩家` = 排除事件玩家（不命中自己）；`空` = 不排除
  - 视线检查：**布尔值** `真`/`假`（⚠️ 不是常量 `表面`，是布尔值）
  - 标准写法：`射线命中位置(矢量(0, 0, 0), 矢量(0, 0, 0), 所有玩家(所有队伍), 事件玩家, 真);`
  - 示例 1（视线检测敌人）：`射线命中位置(眼睛位置(事件玩家), 眼睛位置(事件玩家) + 面朝方向(事件玩家) * 24.500, 所有玩家(对方队伍(所在队伍(事件玩家))), 所有玩家(所在队伍(事件玩家)), 真)`
  - 示例 2（向下射线找地面落点）：`射线命中位置(全局.NextPos, 全局.NextPos + 矢量(0, -100, 0), 空, 空, 真)` —— 从位置向正下方发射 100 米射线找下方第一个表面点，用于抛物线落点检测
  - ⚠️ 起点/终点都是位置矢量，方向 = 终点 - 起点；想向某方向延伸时用 `起点 + 方向 * 距离` 作终点
  - ⚠️ 判断是否命中：用 `Y方向分量(命中位置)` 比较，不要用 `!= 空`（不可靠）
  - ⚠️ **抛物线地面检测的正确逻辑**：球在下落（`Y方向分量(速度) < 0`）且下一帧 Y 低于地面 Y 时触发着地。
    - 单纯比较 `球 Y <= 地面 Y` 会穿过地面（射线命中点总在球下方，条件恒假）
    - 必须加 `Y方向分量(速度) < 0` 前置条件，避免球上升时误判
  - ⚠️ `射线命中位置` vs `最近的可行走位置`：
    - `射线命中位置`：找球正下方真实地面，精度高但需配合速度方向判断
    - `最近的可行走位置`：返回最近的角色可行走路径节点，悬崖外可能返回悬崖边，适合简单地面检测（配合 `Y方向分量(速度) < 0`）
- `上一个文本ID;` —— 在 `创建HUD文本`/`创建地图文本` 之后用于获取刚创建的文本 ID，配合 `消除HUD文本(上一个文本ID)`

## 8. 常量（Constant）

常量可直接在参数中使用。**完整列表见 `references/ow.tmLanguage.json` 的 `constants`**。常用分类：

- **颜色**：`白色`、`黑色`、`红色`、`绿色`、`蓝色`、`黄色`、`紫色`、`橙色`、`灰色`、`天蓝色`、`玫红`、`青绿色`、`水绿色`、`亮紫色`、`灰绿色` 等。**用法**：`颜色(白色)`（用 `颜色()` 包裹）
- **按钮**：`主要攻击模式`、`辅助攻击模式`、`技能1`、`技能2`、`终极技能`、`近身攻击`、`跳跃`、`蹲下`、`装填`、`互动`。**用法**：`按钮(技能1)`（用 `按钮()` 包裹）；`按钮被按下(玩家, 按钮(互动))`
- **队伍**：`队伍1`、`队伍2`、`双方`、`全部`、`所有队伍`
- **真/假/空**：`真`、`假`、`空`、`空数组`、`无`、`全部禁用`、`无动作`
- **比较运算符**：`==`、`!=`、`<=`、`>=`、`<`、`>`
- **英雄**：见 §5.3
- **地图**：`国王大道`、`花村`、`漓江塔`、`监测站：直布罗陀`、`阿努比斯神殿`、`多拉多`、`好莱坞`、`伊利奥斯`、`绿洲城`、`釜山`、`巴黎` 等
- **游戏模式**：`攻防作战`、`运载目标`、`攻击护送`、`占领要点`、`死斗`、`团队死斗`、`闪点作战`、`机动推进`、`勇夺锦旗`、`雪球攻势`、`动感斗球`、`英雄精通：试炼场` 等
- **状态**：`晕眩`、`定身`、`冰冻`、`燃烧`、`沉睡`、`无敌`、`无法杀死`、`相移`、`被入侵`、`击倒`、`击晕`
- **等待条件**（`等待` 第 2 参数）：`无视条件`、`当为"假"时中止`、`当为"真"时中止`、`当为"真"时重新开始`
- **取整模式**（`取整` 第 2 参数）：`上`、`下`、`至最近`
- **方向**（`地图矢量`/`本地矢量` 第 1 参数）：`左`、`右`、`前`、`后`、`上`、`下`
- **旋转模式**（`地图矢量`/`本地矢量` 第 3 参数，**必填**）：`旋转`（仅旋转方向，不改变坐标系）、`旋转并转换`（旋转并转换到世界坐标系）。⚠️ 是常量，不是矢量值
- **视线检查/表面检查**（`范围内玩家` 第 4 参数、`在视线内` 屏障阻挡参数）：`关闭`（不做任何检查，纯距离判定）、`表面`（只检查地形表面）、`屏障不会阻挡视线`、`所有屏障阻挡视线`、`敌方屏障阻挡视线`、`表面及全部屏障`、`表面及敌方屏障`。⚠️ 这是**常量**，不是布尔 `假`/`真`；`关闭` ≠ `假`
- **HUD 位置**（`创建HUD文本` 位置参数）：`左边`、`顶部`、`右边`、`底部`
- **可见性**：`默认可见度`、`始终可见`、`始终不可见`、`可见`、`可见和字符串`、`可见和值`、`可见和颜色`、`可见和位置`、`可见，字符串和颜色`、`可见，值和颜色`、`可见，位置和半径`、`可见，位置，字符串和颜色` 等组合
- **排序规则**：`排序`、`可见性和排序`、`排序规则和字符串`、`排序规则和颜色`、`可见性，排序规则，字符串和颜色` 等
- **效果形状**：`球`、`球弹道`、`光柱`、`光晕`、`环`、`细环`、`宽环`、`星形`、`螺旋`、`箭头`、`箭矢`、`圆圈`、`骷髅`、`感叹号`、`问号`、`对号`、`加号`、`火花`、`火焰` 等
- **截取**（文本/效果参数）：`不要截取`、`根据表面截取`
- **数组操作**：`添加至数组`、`从数组中移除`、`根据值从数组中移除`、`根据索引从数组中移除`
- **相对**（`施加推力` 第 4 参数）：`至地图`（世界坐标系）、`至玩家`（相对玩家朝向）、`至摄像机`（相对摄像机）
- **相反运动**（`施加推力` 第 5 参数）：`合并相反运动`（叠加到现有运动）、`取消相反运动`（取消反方向现有运动）、`取消相反运动XYZ`（XYZ 三轴均取消反方向运动）

## 9. 编写约定（必须遵守）

1. **花括号单独成行**：`{` 和 `}` 必须独占一行，行尾花括号会导致解析错误
2. **缩进**：用制表符，显示大小 4
3. **结尾符号**：事件项 `;`、条件项 `;`、动作项 `;`（条件项用 `;` 与真实导出代码一致；语法亦容忍 `,`）
4. **中文关键词**：结构关键字用中文（`规则`、`事件`、`条件`、`动作`、`变量`、`子程序`、`全局`、`玩家`、`设置`、`禁用`）
5. **控制流关键字用英文**：`If`、`Else If`、`Else`、`End`、`While`、`For 玩家变量`、`For 全局变量`
6. **变量引用**：全局变量 `全局.变量名` 或 `全局[索引]`；玩家变量 `事件玩家.变量名` 或 `玩家.变量名`；数组索引 `全局.变量名[索引]`、`事件玩家.变量名[索引]`
7. **动作调用**：`动作名(参数1, 参数2, ...);`
8. **值函数调用**：`函数名(参数1, 参数2)`（无分号，除非作为动作语句）
9. **颜色、按钮、字符串值必须包裹**：`颜色(白色)`、`按钮(技能1)`，不可直接写常量名；字符串作为「值/参数」时必须用 `自定义字符串("...")` 包裹，不可直接写字面量 `"..."`（详见 §7.10，含适用与不适用场景）
10. **运算符**：支持中缀 `+ - * / % ^`、`+= -= *= /=`、`== != <= >= < >`、`&& || !`、三元 `? :`。⚠️ **取负值用 `-1 * 值`，不可写 `-值`**（见 §7.1），字面量负数（`-3.500`）不受此限
11. **字符串占位符**：`自定义字符串("值: {0}", 全局.Score)`，`{0}`/`{1}`/`{2}` 对应后续参数；未用占位填 `空`
12. **禁用前缀**：`禁用 规则("名") { ... }` 禁用整条规则；`禁用 动作名(...);` 禁用单条动作
13. **循环取值**：`For 全局变量`/`For 玩家变量` 循环体内用 `单次赋值(全局.变量名)` 取当前计数，避免每帧重算
14. **持续规则触发语义**（见 §5.4）：空条件（无 `条件` 块）的持续规则只执行 1 次；有条件时每次条件从假变真执行 1 次。需反复执行请用条件翻转或 `循环`/`While`
15. **标点全英文**（见 §1）：代码结构标点一律英文半角 `()` `,` `;` `"` `:` `==`，禁用全角 `（）` `，` `；` `“”` `：`；字符串内容（引号内显示文本）不受此限
16. **不猜测：优先问，或查证；查无定论则必问**：对不确定的函数名、签名、参数数量/顺序、常量取值、语法结构，**禁止凭记忆猜测**。遇到不确定点时，默认先走路径 A：
    - **路径 A — 先向用户提问（默认首选）**：遇到任何不确定点时，**默认优先用 AskUserQuestion 向用户提问**，无论该问题是客观事实还是用户意图。用户是最权威的信息源，直接问往往比查证更高效、更准确。提问时说明不确定点 + 给出具体选项（含推荐项，推荐项放第一位并标注"推荐"），而非开放反问。用户答复后即可下笔
    - **路径 B — 查证（用户希望快速推进、或问题纯客观且明确可查时再用）**：先查本目录 `references/`（见 §12）：`ow.tmLanguage.json`（动作/值/常量/事件/英雄的完整列表）、`SYMBOL_ACTION.js`（动作参数结构与格式模板）、`SYMBOL_CONDITION.js`（值函数参数与返回类型）、`SYMBOL_CONSTANT.js`（常量）。本地查不到时 WebFetch 获取 `model.ts`：`https://raw.githubusercontent.com/XHanL/overwatch-workshop/main/src/web/model.ts`。需要真实写法示例时，可参照真实工坊导出的 `.ow` 代码（如迷宫逃生模式 `dm.ow`，约 304KB）
    - **查无定论则必问**：若走路径 B 查证后仍不明确（查不到该函数、参数含义模糊、多种合理取法），**不要硬猜**，切到路径 A 向用户提问，附上已查到的信息 + 不确定点请用户决策
    - **宁可慢一步提问/查证，也不要写出可能错误的代码**；确认后再下笔，避免给用户留下"凭印象写"的错误代码徒增返工

## 10. 限制与注意事项（来自仓库 README）

1. 同类插件只能启用一个，否则会破坏功能
2. 语法解析只支持工坊导入时接受的语法，以工坊导出时的格式化风格为准
3. **工坊导出文本与导入语法有差异**，例如 `无` 需改为 `全部禁用`
4. 变量与子程序声明放在文件头部；索引可跳号
5. 商业限制：禁止用于强制赞助收费项目；允许非强制赞助（所有玩家免费平等体验）

## 11. 完整示例

```
设置
{
}

变量
{
    全局:
        0: A
        1: Score
        2: game_0
        3: Time
        4: Workshop
    玩家:
        0: A
        1: Kills
        2: Special_state
        3: NEWPLAYER
}

子程序
{
    0: Sub0
}

规则("Hello World")
{
    事件
    {
        持续 - 全局;
    }
    动作
    {
        创建HUD文本(所有玩家(所有队伍), 自定义字符串("Hello World!", 空, 空, 空), 空, 空, 空, 1, 颜色(白色), 颜色(白色), 颜色(白色), 可见和字符串, 默认可见度);
    }
}

规则("修改移动速度")
{
    事件
    {
        持续 - 每名玩家;
        双方;
        全部;
    }
    动作
    {
        设置移动速度(事件玩家, 150);
    }
}

规则("击杀计数")
{
    事件
    {
        玩家造成最后一击;
        双方;
        全部;
    }
    条件
    {
        是否是机器人(事件玩家) != 真;
    }
    动作
    {
        修改玩家变量(事件玩家, Kills, 加, 1);
        小字体信息(事件玩家, 自定义字符串("击杀: {0}", 事件玩家.Kills));
    }
}

// 真实风格示例：玩家初始化（参照 dm.ow 简化）
规则("玩家初始化")
{
    事件
    {
        持续 - 每名玩家;
        双方;
        全部;
    }
    条件
    {
        是否是机器人(事件玩家) != 真;
        游戏正在进行中 == 真;
        已重生(事件玩家) == 真;
    }
    动作
    {
        等待(0.250, 当为"假"时中止);
        事件玩家.Special_state = 数组(0, 0, 0, 0, 0, 假, 假, 真, 7.500, 假, 0, 0, 0);
        If(所在队伍(事件玩家) == 队伍2);
            设置玩家可选的英雄(事件玩家, 数组(英雄(莱因哈特), 英雄(死神), 英雄(路霸), 英雄(拉玛刹)));
            事件玩家.Special_state[5] = 真;
        Else;
            设置玩家可选的英雄(事件玩家, 数组(英雄(查莉娅), 英雄(回声), 英雄(士兵：76), 英雄(天使), 英雄(安娜)));
        End;
        等待直到(已重生(事件玩家), 5399);
        传送(事件玩家, 数组随机取值(全局.RoomPositions));
        小字体信息(事件玩家, 自定义字符串("长按 {0}+{1} 切换人称", 输入绑定字符串(按钮(蹲下)), 输入绑定字符串(按钮(互动))));
    }
}

// 全局循环 + For 循环 + 索引赋值
规则("游戏计时")
{
    事件
    {
        持续 - 全局;
    }
    条件
    {
        游戏正在进行中 == 真;
    }
    动作
    {
        全局.Time = 全局.Workshop[2];
        For 全局变量(game_0, 0, 全局.Workshop[2], 1);
            等待(1, 无视条件);
            全局.Time -= 1;
            创建HUD文本(本地玩家, 空, 自定义字符串("剩余时间: {0} 秒", 全局.Time), 空, 顶部, 0, 颜色(白色), 颜色(白色), 颜色(白色), 可见和字符串, 默认可见度);
        End;
    }
}

禁用 规则("已禁用的规则")
{
    事件
    {
        持续 - 全局;
    }
    动作
    {
        中止;
    }
}
```

## 12. 参考文件

本 skill 目录下的 `references/` 提供权威数据源，编写代码时可查阅：

| 文件 | 内容 | 用途 |
|---|---|---|
| `references/ow.tmLanguage.json` | TextMate 语法定义（28KB） | 所有动作/值/常量/事件/英雄的**完整权威列表**，忘记某个元素名称时用 Read 搜索此文件 |
| `references/SYMBOL_ACTION.js` | 动作定义示例（含参数、格式、提示） | 查看动作的参数结构、默认值、格式模板 |
| `references/SYMBOL_CONDITION.js` | 值/条件定义示例 | 查看值函数的参数、返回类型、格式 |
| `references/SYMBOL_CONSTANT.js` | 常量定义示例 | 查看比较运算符、队伍等常量 |

**完整数据源**（未包含在本 skill 中，因体积较大 552KB）：仓库 `src/web/model.ts`，包含每个动作/值的完整参数定义、默认值、中文提示。需要某个动作的详细参数时，可通过 WebFetch 获取：
`https://raw.githubusercontent.com/XHanL/overwatch-workshop/main/src/web/model.ts`

**真实代码参照**：真实工坊导出的 `.ow` 代码（如迷宫逃生模式 `dm.ow`，约 304KB），可作为值函数参数、动作签名、控制流写法的权威示例。

## 13. 符号定义格式说明

`SYMBOL_*.js` 与 `model.ts` 中每个符号的定义结构（便于查阅引用文件）：

```js
"动作名": {
    "参数": {
        "参数名": {
            "类型": "条件|变量|数字|玩家变量|变量操作选项|...",
            "默认": "默认值",
            "提示": "参数说明"
        }
    },
    "格式": "全局.${1} = ${2};",   // ${1}、${2} 是第1、第2个参数的占位符
    "标签": ["行动"],               // 分类标签
    "提示": "动作说明"
}
```

值/条件定义多一个 `"返回": ["变量"]` 字段表示返回类型。常量定义最简：`{ "标签": [...], "提示": "..." }`。
